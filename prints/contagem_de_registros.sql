/* ==========================================================
   CONTAGEM DE REGISTROS POR TABELA
   Banco: PTBPABD | Schema: dbo
   ========================================================== */

/* Tabela: dbo.advisor */
SELECT COUNT(*) AS qtd FROM dbo.advisor;
-- Resultado: 2000 registros

/* Tabela: dbo.classroom */
SELECT COUNT(*) AS qtd FROM dbo.classroom;
-- Resultado: 30 registros

/* Tabela: dbo.course */
SELECT COUNT(*) AS qtd FROM dbo.course;
-- Resultado: 200 registros

/* Tabela: dbo.department */
SELECT COUNT(*) AS qtd FROM dbo.department;
-- Resultado: 20 registros

/* Tabela: dbo.instructor */
SELECT COUNT(*) AS qtd FROM dbo.instructor;
-- Resultado: 50 registros

/* Tabela: dbo.prereq */
SELECT COUNT(*) AS qtd FROM dbo.prereq;
-- Resultado: 100 registros

/* Tabela: dbo.section */
SELECT COUNT(*) AS qtd FROM dbo.section;
-- Resultado: 100 registros

/* Tabela: dbo.student */
SELECT COUNT(*) AS qtd FROM dbo.student;
-- Resultado: 2000 registros

/* Tabela: dbo.takes */
SELECT COUNT(*) AS qtd FROM dbo.takes;
-- Resultado: 30000 registros

/* Tabela: dbo.teaches */
SELECT COUNT(*) AS qtd FROM dbo.teaches;
-- Resultado: 100 registros

/* Tabela: dbo.time_slot */
SELECT COUNT(*) AS qtd FROM dbo.time_slot;
-- Resultado: 20 registros